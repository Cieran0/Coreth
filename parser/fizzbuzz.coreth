main() {
    int x = 1
    int max = 100
    int Either
    while( x <= max ) 
    { 
        Either = 0
        if(3 *?= x) { 
            write(0,"Fizz")
            Either = 1
        }
        if(5 *?= x) { 
            write(0,"Buzz") 
            Either = 1
        }
        if(!Either) { 
            printNumber(x)
        }
        write(0,"\n")
        x = x + 1 
    } 
}